export class PseudocodeLine {
    public code: string;
    public stepText: string;

    public constructor(code: string, stepText: string) {
        this.code = code;
        this.stepText = stepText;
    }
}
